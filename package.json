{
    "name": "chatgpt-ux-navigator-monorepo",
    "private": true,
    "workspaces": [
        "server",
        "extension",
        "orchestrator",
        "writer-cli"
    ],
    "scripts": {
        "server": "bun --filter @repo/server start",
        "orchestrator": "bun --filter @repo/orchestrator start",
        "writer-cli": "bun --filter @repo/writer-cli start",
        "test": "bun --filter '*' test",
        "lint": "bun --filter '*' lint",
        "format": "bun --filter '*' format",
        "typecheck": "bun --filter '*' typecheck"
    }
}
